# Зависимости парсера StyleNova
# Установка:  pip install -r requirements.txt
#             playwright install

scrapegraphai>=2.2.4   # ИИ-парсер (требует Python 3.12+)
python-dotenv>=1.0.0   # чтение ключей из файла .env
